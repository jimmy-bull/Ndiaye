import {
    StyleSheet,
    View,
    Dimensions,
    Text,
    TextInput,
    TouchableOpacity,
    Image,
    ScrollView,
    SafeAreaView
} from 'react-native';
import * as React from 'react';
import SearchBlock from './SearchBlock';
import FriendSearchResult from './friendSearchResult';
export default function PostCarousel({ navigation }) {
    return (
        <SafeAreaView style={{ flex: 1 }}>

        </SafeAreaView>
    )
}